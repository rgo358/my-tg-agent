version: '3.8'
services:
  agent:
    build: .
    restart: unless-stopped
    volumes:
      - ./memory:/app/tg_memory_db_v3
    environment:
      - SESSION_STRING=${SESSION_STRING}
      - API_ID=${API_ID}
      - API_HASH=${API_HASH}
      - XAI_API_KEY=${XAI_API_KEY}
