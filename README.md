# TG Super Agent

1. SESSION_STRING, API_ID, API_HASH, XAI_API_KEY — добавь в Variables на Railway.
2. Деплой: New Project → GitHub repo → Deploy.
3. После деплоя агент пришлёт сообщение в "Сохранённые сообщения".